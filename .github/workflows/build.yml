name: Build & Release

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      apk_name: ${{ steps.prepare.outputs.apk_name }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x InstaGrab/gradlew

      - name: Decode signing keystore
        run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > InstaGrab/app/release.jks

      - name: Print signing cert fingerprint
        run: keytool -list -v -keystore InstaGrab/app/release.jks -storepass "${{ secrets.KEYSTORE_PASSWORD }}" -alias "${{ secrets.KEY_ALIAS }}" | grep SHA256

      - name: Build release APK
        working-directory: InstaGrab
        run: |
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=$(pwd)/app/release.jks \
            -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }}

      - name: Build release AAB for Google Play
        working-directory: InstaGrab
        run: |
          ./gradlew bundleRelease \
            -Pandroid.injected.signing.store.file=$(pwd)/app/release.jks \
            -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }}

      - name: Prepare APK asset
        id: prepare
        run: |
          APK_NAME="InstaGrab-${GITHUB_REF_NAME#v}-main.apk"
          cp InstaGrab/app/build/outputs/apk/release/app-release.apk "$APK_NAME"
          cp InstaGrab/app/build/outputs/bundle/release/app-release.aab "InstaGrab-${GITHUB_REF_NAME#v}-main.aab"
          echo "apk_name=$APK_NAME" >> "$GITHUB_OUTPUT"
          MAPPING=InstaGrab/app/build/outputs/mapping/release/mapping.txt
          if [ -f "$MAPPING" ]; then
            cp "$MAPPING" "InstaGrab-${GITHUB_REF_NAME#v}-main-mapping.txt"
          fi

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prepare.outputs.apk_name }}
          path: |
            ${{ steps.prepare.outputs.apk_name }}
            InstaGrab-*-main.aab
            InstaGrab-*-main-mapping.txt
          retention-days: 30

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download APK artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/*.apk
            artifacts/*-mapping.txt
          name: InstaGrab ${{ github.ref_name }}
          generate_release_notes: true
          fail_on_unmatched_files: true
